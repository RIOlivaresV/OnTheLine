<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.example.projectmenu.ui.home.HomeFragment"
        android:label="@string/title_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_navigation_home_to_navigation_prepList"
            app:destination="@id/navigation_prepList" />
    </fragment>

    <fragment
        android:id="@+id/navigation_prepList"
        android:name="com.example.projectmenu.ui.prepList.PrepListFragment"
        android:label="Prep List"
        tools:layout="@layout/fragment_preplist" >
        <action
            android:id="@+id/action_navigation_prepList_to_navigation_stock"
            app:destination="@id/navigation_stock" />
    </fragment>

    <fragment
        android:id="@+id/navigation_stock"
        android:name="com.example.projectmenu.ui.stock.StockFragment"
        android:label="Stock"
        tools:layout="@layout/fragment_stock" />
</navigation>